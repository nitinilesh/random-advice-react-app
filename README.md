# 🧠 Advice Generator App

A simple React application that fetches random advice from a public API and keeps track of how many advice messages the user has read. This project demonstrates core React concepts such as hooks, API calls, component reusability, and state management.

---

## 🚀 Features

- Fetches random advice from an external REST API
- Displays advice dynamically on the UI
- Tracks the number of advice pieces read
- Uses React Hooks (`useState`, `useEffect`)
- Demonstrates component-based architecture
- Handles React 18 Strict Mode behavior correctly

---

## 🛠️ Tech Stack

- **React**
- **JavaScript (ES6+)**
- **CSS**
- **Advice Slip API**

---

## 📚 Concepts Covered

- API integration using `fetch` and `async/await`
- State management with `useState`
- Side effects using `useEffect`
- Functional state updates to avoid stale state issues
- Props for component communication
- React Strict Mode behavior in development

---

## 📦 Project Structure


